export const getRotation = (time: number) => (Math.PI * 2) / (3600 * time);
export const earthRotation = 0.0005;

export const earthSize = 1;
export const earthDistance = 50;
export const sunSize = earthSize * 25;
